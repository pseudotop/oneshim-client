# ONESHIM Design Context

## Users

**Knowledge workers** (developers, designers, PMs, analysts) who spend 6-10+ hours daily at a desktop. ONESHIM runs as a background agent that monitors context (active windows, system metrics, screen captures) and surfaces AI-powered suggestions, coaching, and focus analytics.

Users interact with the product through:
- **Web Dashboard** (localhost:10090) — IDE-style shell for reviewing metrics, focus, reports, coaching history
- **MagicOverlay** — floating Tauri WebView for inline AI suggestions and GUI element detection
- **Control Box / Tracking Panel** — compact always-on status indicators
- **System Tray** — quick access (native, not WebView)

**Context of use**: Users are in the middle of focused work. The UI must never interrupt flow. Information density matters — knowledge workers want data-rich views, not empty marketing screens. The product should feel like a trusted instrument panel, not a chatbot toy.

**Emotional goals**: Confidence (my work is being tracked accurately), Calm (non-intrusive, always there when needed), Intelligence (the system understands my patterns).

## Brand Personality

**Intelligent. Precise. Composed.**

- **Intelligent**: The product analyzes behavioral patterns, predicts focus regimes, and coaches proactively. The UI should reflect analytical competence — clean data visualization, meaningful metrics, purposeful hierarchy.
- **Precise**: Every pixel should feel intentional. No decorative fluff. Spacing, alignment, and typography are tight and systematic.
- **Composed**: Calm under complexity. Even when displaying dense data (heatmaps, process lists, coaching goals), the interface remains visually quiet and organized.

## Aesthetic Direction

**Visual tone**: Technical-professional, IDE-adjacent. Inspired by developer tooling aesthetics — monospace accents for data, compact information density, semantic color over decorative color.

**References**:
- OpenAI Codex (desktop app) — minimum quality bar for polish, spacing, and interaction design
- Linear — information density + visual calm
- VS Code — shell structure (activity bar, side panel, status bar) already adopted
- Raycast — overlay interaction patterns, compact floating UI

**Anti-references**:
- Consumer SaaS dashboards with excessive whitespace and illustration-heavy empty states
- Notion-style soft/playful aesthetics — too casual for the analytical nature of this product
- Rainbow accent colors — brand teal only for primary actions/metrics (per existing feedback)

**Theme**: Light and Dark mode, both fully supported via CSS custom properties. No `dark:` Tailwind prefix anywhere.

**Typography**: System font stack (`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto`). No custom web fonts. Monospace for technical/numeric values.

## Design System (Existing)

ONESHIM has a mature, enforced design token system. **All design work must use existing tokens.**

### Token Architecture

| Layer | Location | Purpose |
|-------|----------|---------|
| CSS Custom Properties | `src/index.css` | RGB triplets for light/dark switching |
| Tailwind Extensions | `tailwind.config.js` | Semantic color classes (`bg-surface-base`, `text-content`) |
| JS Token Objects | `src/styles/tokens.ts` | Composable class strings for components |
| Variant Objects | `src/styles/variants.ts` | Component-specific variant maps |
| Lint Gate | `scripts/lint-design-tokens.sh` | CI rejects hardcoded colors/spacing |

### Color System

- **Brand**: Teal — `brand`, `brand-hover`, `brand-text`, `brand-signal`, `brand-bar`
- **Surface**: `base`, `elevated`, `muted`, `inset`, `sunken`, `overlay`
- **Content**: `DEFAULT`, `secondary`, `tertiary`, `inverse`, `muted`, `strong`
- **Semantic**: `success` (green), `warning` (yellow), `error` (red), `info` (blue)
- **Status**: `connected`, `connecting`, `disconnected`, `error`
- **Border**: `DEFAULT`, `muted`, `strong`
- **Interactive**: `hover`, `active`
- **Chart palette**: Raw hex in `palette` object (teal, blue, violet, amber, red, emerald, indigo, pink)

### Spacing Scale

Constrained: `[0, 1, 2, 3, 4, 6, 8, 12]` Tailwind units. Off-scale values rejected by CI.

### Typography Scale

`h1` (2xl bold) → `h2` (xl semibold) → `h3` (lg semibold) → `h4` (base medium) → `body` (sm) → `caption` (xs) → `micro` (11px) → `nano` (10px). Stat variants: `hero` (3xl), `large` (2xl), `normal` (lg).

### Motion

- Colors: 150ms ease-out
- Transform: 300ms ease-out
- Opacity: 200ms ease-out
- Durations: fast (150ms), normal (300ms), slow (500ms)

### Elevation

- Dropdown: shadow-lg, z-40
- Dialog: shadow-xl, z-50
- Overlay: shadow-2xl, z-50
- Tooltip: shadow-md, z-60
- Detection: z-10000+

### Border Radius

`none` → `sm` (rounded) → `md` (rounded-lg) → `lg` (rounded-xl) → `full`

### Component Primitives (10)

Button, Card, Input, Select, Badge, Tabs, Toast, Spinner, Skeleton, EmptyState — all use `forwardRef`, `cn()`, token imports, variant objects.

## Design Principles

1. **Token-first, always** — Import from `tokens.ts`. Never hardcode `text-slate-500` or `bg-[#fff]`. CI lint enforces this.
2. **Information density over whitespace** — Knowledge workers want data-rich views. Compact layouts, meaningful spacing, no decorative padding.
3. **Brand teal for meaning, not decoration** — Teal signals primary actions, active states, and key metrics. No rainbow accents. Semantic tokens (`success`/`warning`/`error`) for feedback only.
4. **Quiet confidence** — Animations are subtle and functional (150-300ms). No bounces, no attention-seeking effects. Motion serves state transitions, not decoration.
5. **Accessibility by default** — `focus-visible` ring on all interactive elements. WCAG AA contrast minimum. `addon-a11y` in Storybook. Semantic HTML structure.

## Technical Constraints

- **No custom fonts** — system font stack only (performance + native feel)
- **No `dark:` prefix** — all theming via CSS custom properties
- **No arbitrary Tailwind values** — spacing/color must come from token scale
- **Component stories required** — every component needs `*.stories.tsx`
- **Biome linting** — enforced via `pnpm lint`
- **Tailwind 3.4** (not v4) — class-based dark mode
